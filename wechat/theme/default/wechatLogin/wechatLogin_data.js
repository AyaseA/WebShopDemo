$(function() {
    // var $page = $('#wechatLogin_wechatLogin'),
    // pageStr = 'wechatLogin_wechatLogin';

    // var phoneNum = $page.find('#login_phone_num');
    // var verify = $page.find('#login_verify');

    $$.config.hideGlobalMenu();
});
